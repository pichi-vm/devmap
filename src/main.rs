// SPDX-License-Identifier: Apache-2.0

//! The `devmap` command-line tool.
//!
//! Placeholder. The CLI is not yet implemented; it will drive the
//! `devmap-linux` library for device lifecycle and the `devmap-<target>`
//! format crates for on-disk formatting.

fn main() -> std::process::ExitCode {
    eprintln!("devmap: the CLI is not yet implemented");
    std::process::ExitCode::from(2)
}
