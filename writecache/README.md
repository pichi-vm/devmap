# devmap-writecache

Device-mapper `writecache` target parameters and status. Start with `dm::Target`;
use a device-mapper backend such as `devmap-linux` to load it in a table.
This crate does not manage devices or issue device-mapper ioctls.

There are no optional features. See the [API documentation](https://docs.rs/devmap-writecache).
